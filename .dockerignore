# Source-control + editor
.git
.gitignore
.vscode
.idea
.DS_Store

# Local environment
.env
.env.local
.env.*.local

# Build artefacts (rebuilt in the image)
node_modules
.next
out
dist
build

# Test artefacts
coverage
test-results
playwright-report
playwright/.cache

# Logs + dumps
*.log
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*

# OS junk
Thumbs.db

# CI / Docker self-references — not needed inside the image
Dockerfile
.dockerignore
docker-compose.yml
Makefile

# Repo-level docs aren't needed at runtime (kept out for smaller context)
docs
README.md
